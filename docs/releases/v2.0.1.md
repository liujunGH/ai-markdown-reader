# v2.0.1 — 修复与打磨

## 修复

- **YAML frontmatter**：解析前移除 `---\n...\n---` 块，不再被 markdown-it 渲染为普通文本
- **Markdown 内容样式**：补充 v2 渲染层缺失的全局样式（标题层级、列表缩进、段落间距、代码块、表格、引用块、任务列表等）
- **布局层级**：修复文件侧边栏遮挡标签栏的问题（TabBar z-index 提升，侧边栏 z-index 降低）
- **工具栏提示**：所有图标按钮添加中文 tooltip，悬停显示功能说明
- **目录面板**：优化间距、行高与宽度，提升可读性

## 验证

- 单元测试 223 个全绿
- e2e 6 个全绿
- `render-check.mjs` 15/15 渲染正确性验证通过
